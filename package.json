{
  "name": "orient-ink-renderer",
  "version": "0.1.0",
  "private": true,
  "description": "Server-side renderer: Design Studio JSON -> PNG, using Konva-in-Node for parity with the frontend canvas.",
  "scripts": {
    "render": "ts-node src/render-test.ts"
  },
  "dependencies": {
    "canvas": "^2.11.2",
    "konva": "^9.3.20"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}